#include <iostream>

int main()
{
    std::vector<std::vector<std::string>> tokens;
    std::vector<std::string> lines;
    std::string line;
    while (std::getline(std::cin, line)) {
        
    }
}
